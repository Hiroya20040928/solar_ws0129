# Dashboard Quickstart

Run `.\SolarSim.ps1 -Mode sim -Action up` or `.\SolarSim.ps1 -Mode live -Action up` and open the dashboard served by `dashboard_node`.

The static dashboard assets live under `dashboard/`.
